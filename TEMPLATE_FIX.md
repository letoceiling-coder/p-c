# ✅ Исправлены шаблоны админ-панели

## Проблема:
В шаблонах `dashboard.php` и `telegram.php` использовался `$this->urlArray[0]`, который может не существовать после вызова `array_shift()` в методе `getAdminSql()`.

## Решение:

### dashboard.php
Исправлена проверка активного пункта меню:
```php
// Было:
($this->urlArray[0] == 'admin' && !isset($this->urlArray[1])) ? 'active' : ''

// Стало:
(empty($this->urlArray) || (!isset($this->urlArray[0]) && !isset($this->urlArray[1]))) ? 'active' : ''
```

### telegram.php
Исправлены все проверки для работы с массивом после `array_shift()`:
```php
// Для /admin/telegram после array_shift() в urlArray[0] будет 'telegram'
// Было: isset($this->urlArray[1]) && $this->urlArray[1] == 'telegram'
// Стало: isset($this->urlArray[0]) && $this->urlArray[0] == 'telegram'
```

## Логика работы:

1. При обращении к `/admin`:
   - `getAdminSql()` вызывает `array_shift($this->urlArray)` - удаляет 'admin'
   - `urlArray` становится пустым
   - Возвращается шаблон `dashboard`

2. При обращении к `/admin/telegram`:
   - `getAdminSql()` вызывает `array_shift($this->urlArray)` - удаляет 'admin'
   - В `urlArray[0]` остается 'telegram'
   - Метод `telegram()` возвращает шаблон `telegram`

## Проверка:

1. ✅ Синтаксис шаблонов проверен
2. ✅ Файлы обновлены на сервере
3. ✅ Логика работы с массивом исправлена

---

**Шаблоны исправлены!** 🎉
